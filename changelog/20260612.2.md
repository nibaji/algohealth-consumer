# 20260612.2

- 2026-06-12: Fixed appbar/header status bar overlap by adding top safe area insets using `useSafeAreaInsets` across all primary pages and modal headers (`app/index.tsx`, `app/profile.tsx`, `app/family/create.tsx`, `app/family/add-member.tsx`, `app/family/join.tsx`, `app/medical-records/create.tsx`, `app/medical-records/[id].tsx`, `components/medical-records/edit-member-modal.tsx`, `components/medical-records/consult-modal.tsx`).
- 2026-06-12: Created a central `DateInput` component (`components/ui/DateInput.tsx`) to unify date input behavior across the app using `DD-MM-YYYY` format with built-in auto-hyphenation formatting, timezone-safe calendar validation, and future date checks.
- 2026-06-12: Refactored Date of Birth (DOB) and medical record Visit Date input fields across all components and screens to format dates as `DD-MM-YYYY` internally and convert them using `inputDateToApiDate` upon submission. Updated `MedicalRecordCard` and view screens to format and display dates in `DD-MM-YYYY`.
- 2026-06-12: Standardized `KeyboardAvoidingView` configurations across screens and modals. Wrapped all onboarding and medical record creation screens in keyboard avoiding wrappers, and configured behavior to disable adjustments on Android to prevent double-resizing/gap issues (using `behavior={Platform.OS === 'ios' ? 'padding' : undefined}`).
- 2026-06-12: Fixed Consult Modal text input shifting/jumping bug by removing manual status bar padding offsets, aligning with dynamic safe area top/bottom insets, and disabling Android-level keyboard height adjustments to align with the system's `adjustResize` behavior.
- 2026-06-12: Changed consult chatbot user-facing labels and welcome message text to display 'Health Consultant' instead of 'Benish AI' / 'Consult Benish' in `components/medical-records/consult-modal.tsx`.

