## Unreleased
- 2026-06-11: Replaced boilerplate Expo assets with custom, AI-generated premium branding assets matching the AlgoHealth violet/purple theme. Deleted unused boilerplate assets (`react-logo.png`, `partial-react-logo.png`, etc.). Generated new `icon.png`, `splash-icon.png`, `favicon.png`, and Android adaptive icons (`android-icon-foreground.png`, `android-icon-background.png`, `android-icon-monochrome.png`) from generated high-resolution assets, and updated `app.json` configuration background colors to match `#2D0E5A`.
- 2026-06-11: Enhanced homepage focus refresh in `app/index.tsx`. Now updates the user session profile first on focus via `refreshProfile` before pulling family and medical records. Utilizes a ref `isInitialLoad` to only show the full screen loading spinner on the initial mount to prevent layout shifts. Retains current accordion expanded states across refreshes by using a functional state updater.
- 2026-06-11: Implemented cross-platform confirmation support for deletions. Updated `components/medical-records/edit-member-modal.tsx` and `app/medical-records/[id].tsx` to use browser's native `window.confirm` dialogs in web environments (`Platform.OS === 'web'`) since `Alert.alert` is a no-op on React Native Web, and restored the native alerts for iOS/Android.
- 2026-06-11: Updated `components/medical-records/edit-member-modal.tsx` to hide the relationship field selector and custom relationship text input when the family member being edited is the logged-in user themselves (i.e. self). Fixed a bug where the relationship field was hidden for non-self members due to the `user_id` check matching the owner's creator ID. Preserves original relationship value on update submission.
- 2026-06-11: Fixed compilation error in `components/medical-records/member-accordion.tsx` by removing duplicate closing brace.
- 2026-06-11: Implemented homepage editing and deleting of family members. Added get, update, and delete API methods in `src/services/family/familyService.ts`. Developed a premium edit modal `components/medical-records/edit-member-modal.tsx` with robust inline validations and date formatting, and integrated edit triggers and update callbacks in `app/index.tsx` and `components/medical-records/member-accordion.tsx`.
- 2026-06-11: Fixed Date of Birth validation by enforcing a date-only comparison check to correctly block future dates. Enhanced typing UX by appending the hyphen (`-`) immediately upon completing the day (`DD`) and month (`MM`) segments instead of waiting for the next segment's first character, while maintaining natural backspace behavior. Changed in `app/family/add-member.tsx`.
- 2026-06-11: Implemented segment-by-segment change validation and full calendar-check blur validation on the Date of Birth field. Added onBlur and onChange validation handlers for Full Name, Email, and Mobile fields to show inline validation errors dynamically using the built-in TextInput error status. Changed in `app/family/add-member.tsx`.
- 2026-06-11: Updated the add family member form to use DD-MM-YYYY format for Date of Birth with auto-inserted hyphens, renamed the "Relation to Owner" label to "Relationship", and added an optional custom relationship text input when "Other" is selected. Changed in `app/family/add-member.tsx`.
- 2026-06-11: Added an "Add Family Member" CTA button on the homepage dashboard within the active family circle card, linking to `app/family/add-member.tsx`. Changed in `app/index.tsx`.
- 2026-06-11: Disabled ability for a user to join or create a new family circle if they are already in one by hiding the "Circle Settings" settings actions card from the homepage dashboard. Changed in `app/index.tsx`.
- 2026-06-11: Updated authentication type definitions (`src/features/auth/authTypes.ts`) to include the optional `pending_invite` boolean property in `AuthResponse`, and made `is_active` optional in `UserProfileResponse` to match register/login payloads. Changed in `src/features/auth/authTypes.ts`.
- 2026-06-11: Fixed route guard bug where users already in a family circle were redirected to the onboarding screen. Defensively copied `family_id` from the root of `AuthResponse` into the user profile state during session restore, login, and registration. Updated the route layout guarding to redirect family-associated users away from `/onboarding` and `/family` routes back to the homepage. Changed in `src/contexts/AuthContext.tsx`, `src/services/auth/authService.ts`, and `app/_layout.tsx`.
- 2026-06-11: Prioritized the user-friendly "message" field in API error response payloads to prevent displaying raw database exception details to the user. Updated the family member addition CTA from a secondary "Add Another Member" button to a primary "Add" CTA. Changed in `src/services/api/apiClient.ts` and `app/family/create.tsx`.
- 2026-06-11: Implemented AI medical consult chat modal for family members. Added the "Consult" button to the member accordion headers with isolated press zones. Implemented a fullscreen modal chat UI (`components/medical-records/consult-modal.tsx`) using FlashList to query the backend AI endpoint and defensively parse response fields. Changed in `app/index.tsx`, `components/medical-records/member-accordion.tsx`, and `components/medical-records/consult-modal.tsx`.
- 2026-06-11: Implemented family onboarding, profile management, and medical records CRUD features. Added onboarding selector (`app/onboarding.tsx`), create family (`app/family/create.tsx`), and join family (`app/family/join.tsx`) screens, alongside family service layers (`src/services/family/familyService.ts`). Implemented profile details editing screen (`app/profile.tsx`) to update user full name and manage logout. Developed medical record types (`src/features/medical-records/medicalRecordTypes.ts`) and full service client (`src/services/medical-records/medicalRecordService.ts`). Created medical record creation flow (`app/medical-records/create.tsx`) with YYYY-MM-DD date verification and member selection. Built details, edit, and deletion view for medical records (`app/medical-records/[id].tsx`), including showing AI Clinical summaries and native warning alerts. Refactored the dashboard (`app/index.tsx`) to render an accordion of family members and their medical records, migrating the inline accordion and card UI elements into reusable components: `components/medical-records/member-accordion.tsx` and `components/medical-records/medical-record-card.tsx` to align with folder isolation and limit index page file size. Updated route configurations in `app/_layout.tsx` to handle new screen paths. Fixed TypeScript warnings/errors and removed unused imports in profile and creation screens.
- 2026-05-13: Cleaned up remaining unused UI dead code (`icon-symbol.tsx`, `use-color-scheme.ts`, and `reset-project.js`).
- 2026-05-13: Created base UI components (`Typography`, `Button`, `TextInput`) integrated with the custom design system.
- 2026-05-13: Cleaned up Expo boilerplate (`ThemedText`, `ThemedView`, etc) and established barebones `index.tsx` and `_layout.tsx`.
- 2026-05-13: Fixed TypeScript import alias resolution bugs (`@/` -> `@/src/`) and cleaned up `app.json` schemas.
- 2026-05-13: Implemented complete authentication flow (Login, Register, Forgot Password, Reset Password) with automatic token refresh via `expo-secure-store`. Added `AuthContext` for global session state and route guarding.
