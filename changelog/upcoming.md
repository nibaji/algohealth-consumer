## Unreleased
- 2026-06-19: Resolved token storage crashes on Web by implementing a `window.sessionStorage` fallback in `src/services/auth/tokenStorage.ts` when running in web browser contexts, matching sibling `algoscribe` patterns. Native builds continue using `SecureStore` (iOS Keychain / Android Keystore) to ensure maximum cryptographic security.
- 2026-06-19: Replaced direct `rgba` and hex colors in all component stylings and inline properties with centralized theme tokens, adding translucent overlays, state colors (errorDark), and soft shadow configurations to `constants/theme.ts`. Reverified and cleaned up all remaining hardcoded margins, paddings, gaps, and font sizes across `AudioPlayerView`, `ConsultMessage`, `MedicalRecordCard`, `Skeleton`, `AudioNoteRecorder`, and authentication screens.
- 2026-06-19: Comprehensive codebase refactoring to enforce strict typing, centralized design tokens, and React Native best practices. Standardized gender, relationship, blood group, and invite status fields to strict enums. Enhanced the Button component to natively render icons from the centralized IconName enum. Refactored components (Index, CreateFamily, JoinFamily, Onboarding, MemberAccordion, EditMemberModal, AudioNoteRecorder, ConsultInput, MedicalRecordCard, etc.) to use theme spacing, colors, and shadows, replaced inline JSX arrow functions with memoized callbacks, and converted inline mapped lists into memoized subcomponents (e.g., MemberChip, DocumentListItem, AttachmentChip). Renamed kebab-case component and hook files to PascalCase/camelCase to comply with file naming conventions.
- 2026-06-19: Updated the API base URL (`API_BASE_URL`) to use the secure HTTPS production domain `https://app-api.algohealthplus.com`. Changed in `src/utils/config/env.ts`.
