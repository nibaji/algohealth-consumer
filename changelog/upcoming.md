## Unreleased
- 2026-06-20: Added explicit family membership verification on dashboard load. Even when `/users/me` returns a `family_id`, the app now calls `GET /families/me` to confirm the user is actually in the family before loading member data or records. If the verification call fails (backend bug where `family_id` is stale), the family state is cleared and the "no family" onboarding flow is shown instead. `loadFamilyData` now accepts pre-fetched family data to avoid a redundant second network call. Changed in `app/index.tsx`.
- 2026-06-20: Fixed false "email already in use" error when re-inviting a previously deleted family member. The client-side email uniqueness check in the Add Member and Edit Member flows now excludes members with `invite_status === 'rejected'` (deleted/removed members), so their emails are correctly treated as available. Changed in `app/family/addMember.tsx`, `src/features/family/useEditMemberForm.ts`.
- 2026-06-20: Updated family member sort order so pending-invite members always appear at the bottom of the list, the family head (relation = 'Self') always appears near the top even when the logged-in user is the head, and accepted members are sorted alphabetically in between. Changed in `app/index.tsx`.
- 2026-06-20: Pending-invite users (users whose invite is not yet accepted) now see a notice on the dashboard instead of the full member list and management controls. A 'View Invitation' button takes them directly to the InvitesModal. Changed in `app/index.tsx`.
- 2026-06-20: InvitesModal now shows the accepted members of the invited family in a read-only list so users can preview who is in the family before accepting. Added a `getDisplayRelationFromRelation` helper to `src/utils/relation.ts` for contexts where no logged-in user reference is available. Changed in `components/medicalRecords/InvitesModal.tsx`, `src/utils/relation.ts`.
- 2026-06-20: Stop TTS speech immediately when microphone recording starts in the audio note recorder or when the microphone mode is selected in the consult chat input. Changed in `src/features/medicalRecords/useAudioNoteRecording.ts` and `components/medicalRecords/ConsultInput.tsx`.
- 2026-06-20: Fixed FormData file upload issues on React Native Web by converting local and blob file URIs to native standard `Blob` objects using a new `uriToBlob` utility. Changed in `src/utils/file.ts`, `src/features/medicalRecords/useConsult.ts`, `app/medicalRecords/create.tsx`, and `src/services/medicalRecords/medicalRecordService.ts`.
- 2026-06-20: Restructured the family member accordion header layout and added fully UI-thread accordion animations. Migrated `measuredHeight` from React state to a Reanimated shared value and used `runOnUI()` for `onLayout` updates so all worklets (animatedStyle, innerAnimatedStyle) execute purely on the UI thread with zero JS-bridge round-trips. Added a pinned inner `Animated.View` (`innerAnimatedStyle`) that locks the content's height to the measured size during animation, preventing Yoga from re-laying out children on every animated frame — eliminating the content-size-proportional glitch on mobile. Changed in `components/medicalRecords/MemberAccordion.tsx`.
- 2026-06-20: Sorted family members to always show self first, then family head, then others alphabetically. Moved the member Edit button to the accordion header as an icon next to the chevron and enabled it for pending members. Moved the Add Medical Record button to the top of the medical records list. Changed in `app/index.tsx`, `components/medicalRecords/MemberAccordion.tsx`, and `components/medicalRecords/EditMemberModal.tsx`.
- 2026-06-20: Made the family member email address mandatory and unique (preventing duplication among existing family members and the owner's email) in onboarding, member creation, and editing flows. Changed in `app/family/addMember.tsx`, `app/family/create.tsx`, and `src/features/family/useEditMemberForm.ts`.
- 2026-06-20: Unified "self" user detection across the app by updating the `isMemberSelf` utility in `src/utils/relation.ts` to prioritize case-insensitive email matching over `user_id` due to the backend duplicate `user_id` bug. Refactored `useEditMemberForm.ts` to utilize this centralized `isMemberSelf` utility.
- 2026-06-20: Fixed family invitation notification bug where the inviter received the pending invite notification and the invitee did not. Prioritized email-based matching over `user_id` inside the `checkIfFamilyPending` helper in `AuthContext.tsx` to bypass the backend bug where the owner's `user_id` is duplicate-returned for all family members. Added strict null and undefined checks for `user_id` fallback matching.
- 2026-06-20: Fixed TTS voice initialization in Edge and Chromium browsers by asynchronously waiting for the native `voiceschanged` event, and increased the speech timeout guard on web to 30 seconds. Changed in `src/utils/ttsManager.ts`.
- 2026-06-20: Added a copy message feature with a 2-second checkmark confirmation to all chat bubbles containing text, styling the copy button in user messages with translucent colors to blend in cleanly. Changed in `components/medicalRecords/ConsultMessage.tsx`.
- 2026-06-19: Fixed chat messages overflowing the screen width on web in the Consult chat. Replaced percentage-based `maxWidth: '85%'` (unreliable on RN Web) with a pixel-based `width * 0.82` value from `useWindowDimensions`. Added `flexShrink: 1` and `overflow: 'hidden'` to the bubble container so long unbreakable strings wrap instead of expanding past the viewport. Also added `width: '100%'` and `overflow: 'hidden'` to the `chatArea` in `ConsultModal` to prevent horizontal scroll. Changed in `ConsultMessage.tsx`, `ConsultModal.tsx`.
