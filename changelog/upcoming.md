## Unreleased
- 2026-06-11: Implemented family onboarding, profile management, and medical records CRUD features. Added onboarding selector (`app/onboarding.tsx`), create family (`app/family/create.tsx`), and join family (`app/family/join.tsx`) screens, alongside family service layers (`src/services/family/familyService.ts`). Implemented profile details editing screen (`app/profile.tsx`) to update user full name and manage logout. Developed medical record types (`src/features/medical-records/medicalRecordTypes.ts`) and full service client (`src/services/medical-records/medicalRecordService.ts`). Created medical record creation flow (`app/medical-records/create.tsx`) with YYYY-MM-DD date verification and member selection. Built details, edit, and deletion view for medical records (`app/medical-records/[id].tsx`), including showing AI Clinical summaries and native warning alerts. Refactored the dashboard (`app/index.tsx`) to render an accordion of family members and their medical records, including quick-actions to log records for specific members and route navigation headers. Updated route configurations in `app/_layout.tsx` to handle new screen paths. Fixed TypeScript warnings/errors and removed unused imports in profile and creation screens.
- 2026-05-13: Cleaned up remaining unused UI dead code (`icon-symbol.tsx`, `use-color-scheme.ts`, and `reset-project.js`).
- 2026-05-13: Created base UI components (`Typography`, `Button`, `TextInput`) integrated with the custom design system.
- 2026-05-13: Cleaned up Expo boilerplate (`ThemedText`, `ThemedView`, etc) and established barebones `index.tsx` and `_layout.tsx`.
- 2026-05-13: Fixed TypeScript import alias resolution bugs (`@/` -> `@/src/`) and cleaned up `app.json` schemas.
- 2026-05-13: Implemented complete authentication flow (Login, Register, Forgot Password, Reset Password) with automatic token refresh via `expo-secure-store`. Added `AuthContext` for global session state and route guarding.
