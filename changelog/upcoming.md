## Unreleased
- 2026-06-19: Refactored Custom Alert system to add a dedicated `'error'` variant, utilizing `IconName.ExclamationmarkCircleFill` for standard errors, leaving the `'danger'` variant with the `IconName.XmarkCircleFill` (red cross circle) specifically for destructive operations like deletions. Updated all error dialog triggers and migrated raw inline error/success text displays in screens and modals (`create.tsx`, `EditRecordForm.tsx`, `EditMemberModal.tsx`, `InvitesModal.tsx`, `ProfileDetailsCard.tsx`, `PasswordResetCard.tsx`) to premium, horizontal container banners with matching icons from the centralized Icon component.
- 2026-06-19: Added new alert-specific icons (`checkmark.circle.fill` and `xmark.circle.fill`) to the centralized `Icon` component, mapping them to standard Material and SF Symbols. Updated the custom `AlertModal` to display these dedicated icons for the `success` and `danger` / `error` variants instead of general or generic icons.
- 2026-06-19: Added an animated pulsing scale and opacity loader to the static `SplashLoader` in `app/_layout.tsx` using `react-native-reanimated` (running fully on the GPU/UI thread) to provide users with a visual cue during bootloading and session restoration.
- 2026-06-19: Built a custom global Alert modal component (`AlertModal.tsx`) and an `AlertProvider` context with a `useAlert()` hook. Replaced all native system alerts (`Alert.alert`) and browser `window.confirm` / `window.alert` dialogs across the screens (`login`, `register`, `[id]`) and hooks (`useEditMemberForm`, `useAudioNoteRecording`) to provide a high-fidelity, unified custom UI experience.
- 2026-06-19: Aligned the web experience with the sibling `algoscribe` configuration: created `app/+html.tsx` containing metadata, responsive tags, and search engine parameters; custom-generated all Web favicons, touch icons, and manifest images inside the `public/` directory from our own high-resolution app icon (`icon.png`); and updated the app's web favicon reference in `app.json`.
- 2026-06-19: Resolved token storage crashes on Web by implementing a `window.sessionStorage` fallback in `src/services/auth/tokenStorage.ts` when running in web browser contexts, matching sibling `algoscribe` patterns. Native builds continue using `SecureStore` (iOS Keychain / Android Keystore) to ensure maximum cryptographic security.
- 2026-06-19: Replaced direct `rgba` and hex colors in all component stylings and inline properties with centralized theme tokens, adding translucent overlays, state colors (errorDark), and soft shadow configurations to `constants/theme.ts`. Reverified and cleaned up all remaining hardcoded margins, paddings, gaps, and font sizes across `AudioPlayerView`, `ConsultMessage`, `MedicalRecordCard`, `Skeleton`, `AudioNoteRecorder`, and authentication screens.
- 2026-06-19: Comprehensive codebase refactoring to enforce strict typing, centralized design tokens, and React Native best practices. Standardized gender, relationship, blood group, and invite status fields to strict enums. Enhanced the Button component to natively render icons from the centralized IconName enum. Refactored components (Index, CreateFamily, JoinFamily, Onboarding, MemberAccordion, EditMemberModal, AudioNoteRecorder, ConsultInput, MedicalRecordCard, etc.) to use theme spacing, colors, and shadows, replaced inline JSX arrow functions with memoized callbacks, and converted inline mapped lists into memoized subcomponents (e.g., MemberChip, DocumentListItem, AttachmentChip). Renamed kebab-case component and hook files to PascalCase/camelCase to comply with file naming conventions.
- 2026-06-19: Updated the API base URL (`API_BASE_URL`) to use the secure HTTPS production domain `https://app-api.algohealthplus.com`. Changed in `src/utils/config/env.ts`.
