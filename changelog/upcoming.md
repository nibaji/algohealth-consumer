## Unreleased
- 2026-06-11: Updated `eas.json` configuration profiles for `development` and `preview` builds to generate APKs (`buildType: "apk"`) on Android restricted exclusively to `arm64-v8a` CPU architecture (`reactNativeArchitectures=arm64-v8a`) to optimize build times and minimize file size.
